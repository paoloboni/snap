// snap commit <message> — record a dirty working tree as a new patch

// Run the commit command; returns exit code 0/1/2
export async function run(_message: string, _cwd: string): Promise<number> {
  throw new Error("not implemented");
}
