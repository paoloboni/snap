// snap init [path] — create a new empty Snap repository

// Run the init command; returns exit code 0/1/2
export async function run(_path: string, _cwd: string): Promise<number> {
  throw new Error("not implemented");
}
