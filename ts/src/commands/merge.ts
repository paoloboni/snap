// snap merge <repository> — import patches from another repository and replay

// Run the merge command; returns exit code 0/1/2
export async function run(_url: string, _cwd: string): Promise<number> {
  throw new Error("not implemented");
}
